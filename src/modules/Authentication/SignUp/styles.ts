import { StyleSheet } from "react-native";

export const SignUpStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    padding: 30,
  },
  authContainer: {
    width: "100%",
    marginTop: 8,
  },
});
